import { Routes } from '@angular/router';
import { Homepage } from './homepage/homepage'; // Assicurati che il path sia corretto

export const routes: Routes = [
  { path: '', component: Homepage } // '' indica la root del sito
];
